# 项目收尾待办（从当前状态到完整结项）

> 依据：`docs/系统现状评估_基于开题与说明书.md` 与《开题报告》《毕业设计说明书》中的需求基线。  
> **当前聚焦**：环境与数据（B.0）、端到端验收（B.1）、答辩账号与论文证据（B.3 / D～G）。**已完成事项见文末「已完成工作归档」。**

---

## 当前进度与下一步（本机）

**待完成优先顺序**：

| 顺序 | 内容 | 说明 |
|------|------|------|
| 1 | **环境与数据 B.0** | 执行 `docs/sql/` 下 `alter_teacher_area_grade.sql`、`alter_course_address.sql`、`register_roles_parent_teacher.sql`；核对教师 `area_id`/`grade_id`；按 `docs/本地运行说明.md` 启动三端。 |
| 2 | **端到端验收 B.1** | 走通：登录 → 找老师/预约 → 我的订单（课程）→ 后台将课程 `status` 置 **1** → **去评价** → 后台将评价 `status` 置 **1** → 评论区可见；另走 **商品订单** Tab 与 **进步商品 → 确认支付（模拟）**。 |
| 3 | **评价运营闭环** | 管理端审核评价（`status`：`0`→`1`）；论文中写明「展示列表为已通过审核」。 |
| 4 | **B.2 剩余** | 教师审核与论文表述核对；管理端答辩专用账号与菜单权限。 |
| 5 | **阶段 D～G** | 测试用例表、部署与论文证据包、结项自查。 |

**会员卡**：当前策略为**不开放**；论文中可写「后续扩展」或删除相关承诺句，避免与实现矛盾。

---

## 使用说明

- 建议按阶段顺序执行；阶段内可并行（如「测试记录」与「AI 演示」可穿插）。
- 每项前的 `[ ]` 完成后改为 `[x]` 即可跟踪进度；**已归档的完成项**集中在文末，正文不再重复勾选。

---

## 阶段 B：未完成项（环境与验收）

对照需求基线：**注册登录、教师信息发布/审核、找老师、地区/年级/科目筛选、预约课程、订单、商品、会员卡、评价**。

### B.0 环境与数据（联调前必做）

- [ ] 在业务库执行 **`docs/sql/alter_teacher_area_grade.sql`**，保证 `teacher` 表存在 `area_id`、`grade_id`。
- [ ] 执行 **`docs/sql/alter_course_address.sql`**，保证 `course` 表存在 `address`（小程序预约上课地址）。
- [ ] 执行 **`docs/sql/register_roles_parent_teacher.sql`**（小程序注册绑定 `parent`/`teacher` 角色）。
- [ ] 为已有教师数据**补全或核对** `area_id`、`grade_id`（否则按地区/年级筛选可能结果为空）；新入驻数据由小程序表单写入。
- [ ] 按 **`docs/本地运行说明.md`** 启动后端、管理端、小程序，`config.js` 的 `baseUrl` 与真机/模拟器网络可达。

### B.1 验收（代码已就绪，待你本机勾选）

- [ ] **验收**：真机或开发者工具完整走通「登录 → 找老师 → 预约/重复预约拦截 → 我的订单 → 商品下单 → 评价提交 → 管理端审课程与评价 → 评论区可见」并记录问题（若有）。

### B.2 待完成

- [ ] **教师侧 · 审核闭环（操作 + 论文表述）**：管理端将教师 `status` 置为 **1** 后家长端「找老师」才可见（**代码已按 `status=1` 过滤**）；待审/驳回是否在 UI 提示、是否与论文一致，需你本机演示时核对并改文案（若有）。
- [ ] **管理端（PC）**：`ruoyi-ui` 中上述业务菜单齐全、数据可维护；为答辩准备**专用账号**及菜单权限（家长/教师可用小程序注册 + SQL 角色，管理员仍用后台账号），能配合小程序演示全流程。

### B.3 阶段 B 完成判定（可自查）

- [ ] 有一条 **5 分钟级** 演示路径：登录 → 找老师 → 预约 → **我的订单（课程 + 可选商品模拟支付）** → 后台确认课程 → **去评价** → 后台审核评价 → **评论区**；会员卡已停用可跳过。
- [ ] 说明书/开题中写到的移动端功能，在代码中均有对应页面与接口，且无「仅静态原型」页面混在正式 `pages.json` 主路径中。

### B.4 小程序与管理端同步检查（对照表）

以下指 **同一后端接口下**，uni-app（`uniapp/juxing-ruoyi`）与 `ruoyi-ui/src/views/my/*` 字段、数据流是否对齐。

| 业务 | 小程序 | 管理端 | 结论 |
|------|--------|--------|------|
| **教师** | 入驻 `POST /system/teacher/app/join`；列表 `GET /system/teacher/list` 带 `status=1`；`areaId`/`gradeId`/`subjectId` | `my/teacher/index` 含 **地区/年级**、认证状态筛选与审核 | **已同步**（以库表与字典为准） |
| **课程/预约** | `POST /system/course`（**重复预约后端拒绝**）、`GET /system/course/list?studentId=`；`startDate`+`address` | `my/course/index` **上课地址**；**`status=1`** 后家长才可评价 | **已同步** |
| **商品** | `GET /system/product/list`；**下单** `POST /system/order/app/product`（mock） | `my/product`；`my/order` | **已同步** |
| **评价** | **提交** `POST /system/review/app/submit`；**展示** `GET /system/review/app/list`（仅 `status=1`） | `my/review`：`0`→`1` 后评论区可见 | **已同步** |
| **订单 order 表** | 「我的订单」商品 Tab + **模拟支付** | `my/order`；**`course_id` 存 `productId`** | **已对接**（论文说明 mock） |
| **会员卡** | 入口 **toast**；子页 **占位** | `my` 下会员卡配置 | **已停用占位** |
| **注册/角色** | `POST /register/app`（`parent`/`teacher`）；401/403 请求层处理 | `sys_role` + `register_roles_parent_teacher.sql` | **已同步** |
| **AI 助手** | 首页 → `pages/aiAssistant` → `POST /system/ai/assistant` | `ruoyi-ai` + 环境变量密钥 | **已同步**（无密钥则规则回退） |

**结论**：主业务链路与注册、AI 已在代码侧对齐；**剩余**主要是 **B.0 库表执行与数据核对**、**B.1/B.3 验收**、**答辩账号与论文材料（D～G）**。

---

## 阶段 D：测试与质量（支撑论文「测试」章节）

- [ ] **关键用例（建议手工 + 记录表）**：登录、找老师、预约、下单、评价 —— 每类至少 **成功 + 失败** 各 1 条，记录步骤、数据、结果、截图或缺陷编号。
- [ ] **（可选）接口/集成测试**：对核心接口编写最小自动化测试或 Postman 集合，便于复查回归。
- [ ] **权限与安全自检**：越权访问、未登录访问敏感接口；管理端与小程序端分别抽样。
- [ ] **兼容性**：目标小程序基础库版本、真机抽测记录（至少 1 台 iOS + 1 台 Android，若条件允许）。

---

## 阶段 E：部署与可复现交付

- [ ] **数据库**：提供可执行的 SQL 脚本或说明（初始化库、必要字典/菜单）；敏感数据脱敏。
- [ ] **部署说明**：后端 jar 或容器方式、前端构建产物、Nginx/HTTPS（若需要）、小程序上传与体验版/正式版说明。
- [ ] **配置文件模板**：`application-xxx.yml` 示例（不含密码）；小程序 `config.js` 生产 `baseUrl` 切换说明。

---

## 阶段 F：论文与答辩证据包（与代码、说明书一致）

- [ ] **模块清单**：后端各 `ruoyi-*` 模块职责一段说明。
- [ ] **接口清单**：核心业务流程涉及的主要 API 列表（路径、方法、简要说明）。
- [ ] **测试记录**：阶段 D 的用例表汇总（可附在附录或单独 xlsx/md）。
- [ ] **性能或体验（按需）**：关键接口耗时截图、或简单压测说明（与说明书中的指标表述对齐，避免空洞）。
- [ ] **说明书/论文同步修订**：系统结构图、流程图与最终实现一致；AI、测试、部署三节与阶段 C、D、E 产出一致。

---

## 阶段 G：结项检查（「完整结束」最低标准）

- [ ] 三端（管理端、家长小程序、教师小程序）各有一条 **5 分钟内可讲完** 的演示路径且无阻塞性 Bug。
- [ ] 仓库无大块误提交构建产物；README 或 `docs` 内可找到「如何运行 + 如何演示 AI」（参见 `本地运行说明.md`、`阶段C_AI演示说明.md`）。
- [ ] 论文、开题、说明书与系统功能表述一致，无已删除功能的陈旧描述。
- [ ] 源代码已备份；远程仓库（如 GitHub）与本地标签或 release 说明（可选）一致。

---

## 参考文档

- `docs/系统现状评估_基于开题与说明书.md`
- `docs/毕业设计说明书.txt` / `docs/毕业设计说明书.docx`
- `docs/孙浩开题报告-设计专用(1).txt` / 对应 docx
- `docs/本地运行说明.md`、`docs/阶段C_AI演示说明.md`

---

## 已完成工作归档（按时间线汇总，便于结项写进论文）

以下条目**代码或文档已落地**，验收与数据执行仍以你本机为准。

### 阶段 A：工程与仓库规范

- [x] **后端 `pom.xml`**：删除重复的 `<module>` 声明（`ruoyi-area`、`ruoyi-users` 各仅保留一处），全量 `mvn` 编译通过。
- [x] **小程序源码清理**：删除或移出正式工程外的副本/草稿页；确认 `pages.json` 中无引用废弃路径。
- [x] **统一移动端入口**：仅保留一套登录流程与一套「找老师/教师列表」主页面。
- [x] **构建产物与依赖**：`unpackage/`、`node_modules/` 等纳入 `.gitignore`；文档说明本地重新构建。
- [x] **环境配置文档**：`docs/本地运行说明.md`（含 JWT/403/AI 环境变量、注册角色 SQL 说明等）。

### 阶段 B：已落地（代码侧）

- [x] **工程**：`Teacher` 与 `TeacherMapper` 支持 `areaId`/`gradeId`；列表可按地区、年级、科目、姓名筛选。
- [x] **小程序 · 找老师**：三筛 + `/system/teacher/list`，**仅 `status=1`**。
- [x] **小程序 · 老师入驻**：`teacherJoin` + **`POST /system/teacher/app/join`**；待审 `status=0`；管理端审核通过/驳回。
- [x] **小程序 · 登录页**：去掉预填测试账号。
- [x] **小程序 · 预约**：`POST /system/course`；`address` 承载时段与地址；重复预约后端拦截。
- [x] **后端 · 课程**：`address` 字段；`CourseController.add` 补 `studentId`、默认字段。
- [x] **小程序 · 我的订单**：课程 Tab + 商品 Tab；**去评价**。
- [x] **后端 · 评价**：`POST /system/review/app/submit`、`GET /system/review/app/list`（仅已通过）。
- [x] **后端 · 商品订单**：`POST /system/order/app/product`（mock）、`GET /system/order/app/list`。
- [x] **会员卡**：停用占位（toast + 占位页）。
- [x] **管理端教师**：`my/teacher/index` 地区/年级、**认证状态**筛选与快捷通过/驳回。
- [x] **注册与角色（B.2）**：`RegisterBody.registerRole`；`registerUser` 写 `sys_user_role`；`POST /register`（可省略角色则 `common`）；`POST /register/app`（必选 `parent`/`teacher`）；Security 放行 `/register/app`；小程序注册页角色选择 + **403** 处理。
- [x] **预约深化**：同学+师+日重复校验（排除已取消）；时段级冲突、真实支付未做（可按论文取舍）。

### 阶段 C：AI 智能匹配/问答

- [x] **方案**：智能选课助手 + `docs/阶段C_AI演示说明.md`；密钥 `RUOYI_AI_API_KEY`（勿入库）。
- [x] **后端**：`ruoyi-ai` 模块，`AiTutorServiceImpl`（OpenAI 兼容 API + 规则回退）。
- [x] **接口**：`POST /system/ai/assistant`（需登录）。
- [x] **小程序**：首页「智能选课助手」→ `pages/aiAssistant/aiAssistant.vue`。

### 其他已推送仓库的能力（摘要）

- 教师入驻与管理端审核闭环、课程 `address` 与 SQL 脚本、`ruoyi-ai` 模块、注册角色 SQL、文档更新等已合并进远程仓库（以 `git log` 为准）。

---

*文档版本：2026-04-01 — 结构调整：正文仅保留未完成项；已完成项移至文末归档。*
