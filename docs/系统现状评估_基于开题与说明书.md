# 系统现状评估（基于《开题报告》与《毕业设计说明书》）

## 1. 评估范围与依据
- 文档依据：
  - `C:\Users\DELL\Desktop\孙浩开题报告-设计专用(1).doc`
  - `C:\Users\DELL\Desktop\毕业设计说明书.doc`
- 代码依据：
  - `C:\Users\DELL\Desktop\E\master\RuoYi-Vue-master`（后端 + 管理端）
  - `C:\Users\DELL\Desktop\E\uniapp\juxing-ruoyi`（uni-app 小程序端）

> 说明：两份 doc 为旧式二进制格式，转换后存在中文编码噪声，但核心需求（角色、模块、技术栈、目标功能）可明确识别并用于对照评估。

## 2. 文档目标（需求基线）
从两份文档提取的核心目标如下：

- 角色模型：管理员（PC）+ 家长端（小程序）+ 教师端（小程序）
- 核心业务：注册登录、教师信息发布/审核、找老师、按地区/年级/科目筛选、预约课程、订单管理、商品购买、会员卡、评价
- 技术路线：RuoYi（Spring Boot + Vue）+ uni-app + MySQL + MyBatis
- 特色目标：引入 AI 智能匹配/问答能力，提升供需匹配效率
- 工程目标：形成可部署可演示的完整系统，并完成测试与论文支撑材料

## 3. 当前系统实现进度（代码对照）

### 3.1 后端总体进度：**较高（约 75%~85%）**
- 已形成若依多模块结构，且业务模块较齐全：
  - `ruoyi-teacher`、`ruoyi-student`、`ruoyi-course`、`ruoyi-order`、`ruoyi-product`、`ruoyi-card`
  - `ruoyi-grade`、`ruoyi-subject`、`ruoyi-area`、`ruoyi-review`、`ruoyi-notification`、`ruoyi-promotion`、`ruoyi-users` 等
- 多数业务模块具备典型 CRUD 分层（`controller/service/mapper/domain`）。
- 管理端与后端 API 有对应关系（`ruoyi-ui/src/api/my/*.js` 与 `ruoyi-ui/src/views/my/*/index.vue` 基本齐全）。

### 3.2 管理端（Vue）进度：**较高（约 75%~85%）**
- `ruoyi-ui/src/views/my/` 下已覆盖教师、学生、课程、排课、订单、商品、会员卡、评价、通知等页面。
- 适合作为答辩演示中的“后台管理端能力”。

### 3.3 小程序端（uni-app）进度：**中等偏高（约 60%~75%）**
- 路由与页面已覆盖主流程（`pages.json`）：
  - 登录/注册、首页、找老师、教师详情、预约、商品、订单、会员卡、评论、教师入驻、我的
- API 已拆分到 `api/teacher`、`api/product`、`api/card`、`api/review`、`api/teacherJoin` 等目录。
- 结论：主功能“可跑通框架”已具备，但部分页面存在原型化/半成品痕迹。

## 4. 冗余模块与冗余资产（重点）

### 4.1 构建配置冗余（后端）
- `master/RuoYi-Vue-master/pom.xml` 中模块声明重复：
  - `ruoyi-area` 出现 2 次
  - `ruoyi-users` 出现 2 次
- 风险：构建可读性差，后续维护容易误判依赖边界。

### 4.2 小程序源码冗余（明显）
- 发现副本/临时页面：
  - `pages/teacherJoin/teacherJoin - 副本.vue`
  - `pages/product/product - 副本 - 副本.vue`
  - `pages/findteacher/findteacher/findteacher123.vue`
  - `pages/login2.vue`
  - `pages/work/index.vue`（`pages.json` 中已注释，不在正式导航链）
- 结论：存在“历史调试文件未清理”问题，建议归档或删除，避免演示与上线包混淆。

### 4.3 构建产物冗余（仓库体积）
- `uniapp/juxing-ruoyi/unpackage/` 存在大量编译产物，应视为可再生文件。
- `ruoyi-ui/node_modules`、`.cache` 等也属于本地依赖缓存，不应参与项目核心评估内容。

## 5. 未完成或薄弱环节（按优先级）

### P0（答辩/验收前必须补齐）
- **AI 能力落地证据不足**  
  文档强调“AI 智能匹配/问答”，但在业务源码中未看到明确的 AI 服务接入链路（独立 AI service、调用 SDK、模型配置、提示词与回退策略等）。
- **小程序部分页面逻辑未闭环**  
  例如 `findteacher123.vue` 中有明显半成品迹象（示例数据与未完成加载逻辑并存），不宜作为正式页面。
- **模块重复声明需清理**  
  `pom.xml` 重复模块应立即修正，避免后续构建/审查歧义。

### P1（可答辩后补，但建议尽快）
- **系统测试材料不足**  
  业务层自动化测试证据弱（项目内可见测试样例主要来自依赖包/框架示例，不是业务测试）。
- **流程一致性待强化**  
  小程序端存在 `login` 与 `login2` 并存，可能导致登录入口与鉴权流程维护成本增加。

### P2（优化项）
- **目录整洁度**  
  副本文件、历史草稿页面、构建产物建议统一通过规范清理与忽略规则管理。
- **文档与代码一致性**  
  论文中“性能指标、AI能力、测试结果”建议补充为可复现实证（压测截图、接口耗时、AI匹配示例）。

## 6. 与文档目标的匹配度结论
- **已匹配（较好）**：角色划分、核心业务模块框架、后台管理能力、移动端主流程页面。
- **部分匹配**：端到端业务闭环（部分页面仍有半成品特征）。
- **未充分匹配**：AI 智能匹配/问答的工程化落地证据、系统化测试证明。

综合判断：当前系统处于**“主体功能基本成型，进入收尾与证据补强阶段”**。

## 7. 建议的收尾清单（可直接执行）
1. 清理冗余：删除或归档副本页面；修复 `pom.xml` 重复模块。
2. AI最小可用闭环：新增 AI 服务层 + 一个可演示场景（例如“根据科目/年级/地区推荐教师”）。
3. 补测试：至少补充登录、找老师、预约、下单、评价 5 条关键用例（含成功/失败分支）。
4. 统一入口：明确小程序唯一登录页与唯一教师列表页，避免双版本并行。
5. 形成答辩证据包：模块清单、接口清单、测试记录、性能记录、AI演示记录各 1 份。

---

如需，我可以在下一步直接给你一份“可提交版”的《冗余清理清单 + 未完成任务分解（按天排期）》。
