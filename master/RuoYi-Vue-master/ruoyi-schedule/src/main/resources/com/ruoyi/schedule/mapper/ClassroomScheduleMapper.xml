<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.schedule.mapper.ClassroomScheduleMapper">
    
    <resultMap type="ClassroomSchedule" id="ClassroomScheduleResult">
        <result property="scheduleId"    column="schedule_id"    />
        <result property="classroomId"    column="classroom_id"    />
        <result property="weekday"    column="weekday"    />
        <result property="startTime"    column="start_time"    />
        <result property="endTime"    column="end_time"    />
        <result property="status"    column="status"    />
        <result property="createdAt"    column="created_at"    />
    </resultMap>

    <sql id="selectClassroomScheduleVo">
        select schedule_id, classroom_id, weekday, start_time, end_time, status, created_at from classroom_schedule
    </sql>

    <select id="selectClassroomScheduleList" parameterType="ClassroomSchedule" resultMap="ClassroomScheduleResult">
        <include refid="selectClassroomScheduleVo"/>
        <where>  
            <if test="classroomId != null  and classroomId != ''"> and classroom_id = #{classroomId}</if>
            <if test="weekday != null "> and weekday = #{weekday}</if>
            <if test="startTime != null "> and start_time = #{startTime}</if>
            <if test="endTime != null "> and end_time = #{endTime}</if>
            <if test="status != null "> and status = #{status}</if>
            <if test="createdAt != null "> and created_at = #{createdAt}</if>
        </where>
    </select>
    
    <select id="selectClassroomScheduleByScheduleId" parameterType="String" resultMap="ClassroomScheduleResult">
        <include refid="selectClassroomScheduleVo"/>
        where schedule_id = #{scheduleId}
    </select>

    <insert id="insertClassroomSchedule" parameterType="ClassroomSchedule" useGeneratedKeys="true" keyProperty="scheduleId">
        insert into classroom_schedule
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="classroomId != null and classroomId != ''">classroom_id,</if>
            <if test="weekday != null">weekday,</if>
            <if test="startTime != null">start_time,</if>
            <if test="endTime != null">end_time,</if>
            <if test="status != null">status,</if>
            <if test="createdAt != null">created_at,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="classroomId != null and classroomId != ''">#{classroomId},</if>
            <if test="weekday != null">#{weekday},</if>
            <if test="startTime != null">#{startTime},</if>
            <if test="endTime != null">#{endTime},</if>
            <if test="status != null">#{status},</if>
            <if test="createdAt != null">#{createdAt},</if>
         </trim>
    </insert>

    <update id="updateClassroomSchedule" parameterType="ClassroomSchedule">
        update classroom_schedule
        <trim prefix="SET" suffixOverrides=",">
            <if test="classroomId != null and classroomId != ''">classroom_id = #{classroomId},</if>
            <if test="weekday != null">weekday = #{weekday},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createdAt != null">created_at = #{createdAt},</if>
        </trim>
        where schedule_id = #{scheduleId}
    </update>

    <delete id="deleteClassroomScheduleByScheduleId" parameterType="String">
        delete from classroom_schedule where schedule_id = #{scheduleId}
    </delete>

    <delete id="deleteClassroomScheduleByScheduleIds" parameterType="String">
        delete from classroom_schedule where schedule_id in 
        <foreach item="scheduleId" collection="array" open="(" separator="," close=")">
            #{scheduleId}
        </foreach>
    </delete>
</mapper>